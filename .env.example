# Copy this file to .env for local development only. Do not commit your real secrets.

# Flask
SECRET_KEY=change-me

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database (prefer DATABASE_URL in production)
# Example Neon Postgres URL:
# postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
DATABASE_URL=

# Optional granular DB settings for local MySQL (dev only)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ecofinds
